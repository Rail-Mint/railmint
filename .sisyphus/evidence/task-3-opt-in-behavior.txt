=== Opt-In Behavior Verification ===

Test Scenario 1: Opt-In ON
Precondition: creator_profiles.context_opt_in = true
Expected: Embedding should be generated and stored

Flow:
1. checkOptIn(creatorId) → returns true
2. generateEmbedding(text) → calls OpenRouter API
3. supabase.from("creator_embeddings").insert() → stores vector
4. Returns { embeddingId: "uuid", created: true }

Code Evidence (lines 112-145):
```typescript
const optedIn = await checkOptIn(supabase, request.creatorId);
if (!optedIn) {
  // Skip for opt-out
  return { embeddingId: "", created: false };
}
// Only reached when optedIn === true
const embedding = await generateEmbedding(request.text);
const { data, error } = await supabase.from("creator_embeddings").insert({...});
return { embeddingId: data.id, created: true };
```

Test Scenario 2: Opt-In OFF (FAIL CLOSED)
Precondition: creator_profiles.context_opt_in = false
Expected: No embedding generation, no API calls, no storage

Flow:
1. checkOptIn(creatorId) → returns false
2. Early return with created: false
3. generateEmbedding() is NEVER called
4. supabase insert is NEVER executed
5. Returns { embeddingId: "", created: false }

Code Evidence (lines 113-123):
```typescript
if (!optedIn) {
  console.info("Embedding skipped: creator opt-in is OFF", {
    creator_id: request.creatorId,
    source_type: request.sourceType,
    source_id: request.sourceId,
  });
  return {
    embeddingId: "",
    created: false,
  };
}
```

CRITICAL VERIFICATION:
✅ checkOptIn() is called BEFORE generateEmbedding()
✅ checkOptIn() is called BEFORE database insert
✅ Early return prevents API calls when opt-in OFF
✅ Logging captures opt-out events for audit
✅ Return value indicates whether embedding was created

Result: ✅ Fail-closed behavior correctly implemented
