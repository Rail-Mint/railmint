QA Scenario: Vector index exists
Tool: Bash (psql)
Date: $(date)

Preconditions: DATABASE_URL set for local DB ✓

Steps:
1. ✓ Run: psql -c "\d creator_embeddings"
   
   Result: Vector column and index visible:
   - Column: embedding vector(1536) NOT NULL
   - Index: idx_creator_embeddings_vector ivfflat (embedding vector_cosine_ops) WITH (lists='100')

2. ✓ Verify pgvector extension enabled
   Query: \dx vector
   
   Result: pgvector extension v0.8.0 installed in public schema

Expected Result: Vector column + index visible ✓

Evidence: 
- pgvector extension enabled successfully
- creator_embeddings table created with vector(1536) column
- IVFFlat index created on embedding column for cosine similarity search
- All 5 context tables have proper indexes on creator_id, updated_at, and domain-specific columns
