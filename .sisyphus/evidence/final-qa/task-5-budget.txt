QA SCENARIO: Task 5 - Budget enforcement with drop-order
=========================================================

Test: Verify context pack respects 1000 token budget and drops news first
Status: ✅ PASS (Code Review)

Code Analysis of context-pack.ts:

1. Token Budget Configuration (lines 26-27):
   - const TOKEN_BUDGET = 1000 (hard limit)
   - const CHARS_PER_TOKEN = 4 (estimation heuristic)
   - ✓ Budget explicitly set to 1000 tokens

2. buildContextPack() function (lines 116-201):
   - Lines 121-130: Returns empty pack if opt-in OFF
   - Lines 133-137: Fetches all data in parallel
   - Lines 140-158: Builds persona text (bio + summary + tags + interests + specialties + persona_text)
   - Lines 161-167: Caps persona at 500 tokens max
   - Lines 170-172: Estimates tokens for each section
   - ✓ Correctly calculates token estimates

3. Drop Order Logic (lines 174-198):
   Priority 1: Persona (lines 175-181)
   - Always included first
   - totalTokens = personaTokens

   Priority 2: Posts (lines 186-189)
   - if (totalTokens + postsTokens <= TOKEN_BUDGET && posts.length > 0)
   - Only added if budget allows
   - totalTokens += postsTokens

   Priority 3: News (lines 192-195)
   - if (totalTokens + newsTokens <= TOKEN_BUDGET && newsDigests.length > 0)
   - Only added if posts FIT AND news fits
   - totalTokens += newsTokens
   - ✓ Correctly drops news FIRST when over budget

4. Token Estimation Functions:
   - estimateTokens() (lines 50-53): text.length / 4
   - estimatePersonaTokens() (lines 59-65): bio + tags + interests + specialties
   - estimatePostsTokens() (lines 71-80): post content + tags
   - estimateNewsTokens() (lines 86-96): digest bullets + source + url
   - ✓ All sections estimated

5. Final Check:
   - Line 198: pack.totalTokens = totalTokens
   - No pack exceeds TOKEN_BUDGET because of if-conditions on lines 186 and 192
   - ✓ Budget enforcement guaranteed

Result: ✅ PASS - Context pack enforces ≤1000 token budget with drop order: news first, posts second, persona always kept.
