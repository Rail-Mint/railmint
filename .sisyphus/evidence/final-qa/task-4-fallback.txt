QA SCENARIO: Task 4 - Fallback retrieval path
==============================================

Test: Verify recency-based retrieval when embeddings missing
Status: ✅ PASS (Code Review)

Code Analysis of retrieval.ts:

1. retrieveFallbackContext() function (lines 188-238):
   - Line 193: Applies limit (default: HISTORY_CAP_POSTS = 20)
   - Line 197: Calculates history cutoff (90 days ago)
   - Lines 200-206: Queries posts table with filters:
     - .eq("creator_id", creatorId) - filter by creator
     - .gte("created_at", cutoffDate) - 90-day window
     - .order("created_at", { ascending: false }) - most recent first
     - .limit(limit) - max 20 posts
   - Lines 211-214: Optional tag filtering (if tags provided)
   - ✓ Correctly implements recency-based fallback

2. retrieveContext() main function (lines 248-299):
   - Lines 256-260: Checks if creator has any embeddings
   - Line 271-275: If count === 0, uses fallback immediately
   - Lines 279-284: Tries vector search first
   - Lines 287-291: If vector returns empty, uses fallback
   - Lines 295-297: Catches errors and falls back to recency
   - ✓ Correctly implements automatic fallback logic

3. Fallback Conditions:
   - No embeddings exist for creator
   - Vector search returns empty results
   - Vector search fails with error
   - ✓ All fallback paths covered

4. History Caps in Fallback:
   - Line 197: cutoffDate = 90 days ago
   - Line 206: .limit(limit) = 20 posts max
   - Line 223: truncated flag set when limit reached
   - ✓ Same caps as vector path (20 posts OR 90 days)

Result: ✅ PASS - Fallback retrieval uses recency + tags with proper caps.
Automatic fallback from vector→recency is correctly implemented.
